<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.mapper.MemberMapper">

  <insert id="insertMember">
    INSERT INTO TBL_MEMBER (USER_ID, USER_PW, USER_NAME, USER_EMAIL)
    VALUES (#{userId}, #{userPw}, #{userName}, #{userEmail})
  </insert>

  <insert id="insertAuth">
    INSERT INTO TBL_MEMBER_AUTH (USER_ID, AUTH)
    VALUES (#{userId}, 'ROLE_MEMBER')
  </insert>

  <resultMap type="org.zerock.domain.MemberVO" id="memberMap">
    <id property="userId" column="user_id"/>
    <result property="userId" column="user_id"/>
    <result property="userPw" column="user_pw"/>
    <result property="userName" column="user_name"/>
    <result property="userEmail" column="user_email"/>
    <result property="regDate" column="reg_date"/>
    <collection property="authList" resultMap="authMap"></collection>
  </resultMap>

  <resultMap type="org.zerock.domain.AuthVO" id="authMap">
    <result property="userId" column="user_id"/>
    <result property="auth" column="auth"/>
  </resultMap>

  <select id="read" resultMap="memberMap">
    SELECT 
      mem.user_id, mem.user_pw, mem.user_name, mem.user_email, mem.reg_date,
      auth.auth
    FROM 
      tbl_member mem 
      LEFT OUTER JOIN tbl_member_auth auth ON mem.user_id = auth.user_id 
    WHERE 
      mem.user_id = #{userId}
  </select>
  
  <select id="checkId" resultType="int">
    SELECT COUNT(*) 
    FROM tbl_member 
    WHERE user_id = #{userId}
  </select>

</mapper>